(function () {

    var type = net.yy.google.chart.type;
    var util = net.yy.google.chart.util;

    type.BaseBarChart = function() {

    };

    util.extend(type.BaseBarChart, type.BaseChart, {
    });

})();
