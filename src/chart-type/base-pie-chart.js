(function () {

    var type = net.yy.google.chart.type;
    var util = net.yy.google.chart.util;

    type.BasePieChart = function () {

    };

    util.extend(type.BasePieChart, type.BaseChart);

})();
