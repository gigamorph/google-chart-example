(function () {

    var type = net.yy.google.chart.type;
    var util = net.yy.google.chart.util;

    type.BaseLineChart = function () {
    };

    util.extend(type.BaseLineChart, type.BaseChart, {});

})();
